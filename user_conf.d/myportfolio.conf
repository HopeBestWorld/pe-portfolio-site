# Fix: Use 1r/m (1 request per minute)
limit_req_zone $binary_remote_addr zone=timeline_limit:10m rate=1r/m;

server {
    listen 80;
    server_name hope-pe-portfolio.duckdns.org;

    if ($host = hope-pe-portfolio.duckdns.org) {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name hope-pe-portfolio.duckdns.org;

    location / {
        proxy_pass http://myportfolio:5000/;
    }

    # Apply rate limit to POST /api/timeline_post
    location = /api/timeline_post {
        limit_req zone=timeline_limit;
        proxy_pass http://myportfolio:5000/api/timeline_post;
    }

    # Load certificate files
    ssl_certificate /etc/letsencrypt/live/myportfolio/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/myportfolio/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/myportfolio/chain.pem;
}